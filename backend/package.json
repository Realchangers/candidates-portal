{
  "name": "candidates-portal-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "sls offline start",
    "build": "sls package",
    "test": "jest .",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "dynamodb:install": "sls dynamodb install",
    "dynamodb:remove": "sls dynamodb remove",
    "deploy": "sls deploy",
    "deploy:graphql": "sls deploy -f graphql",
    "deploy:publisher": "sls deploy -f jobPublisher"
  },
  "dependencies": {
    "graphql": "^14.0.2",
    "graphql-relay": "^0.5.5"
  },
  "devDependencies": {
    "aws-sdk": "^2.437.0",
    "jest": "^23.6.0",
    "serverless-dynamodb-local": "^0.2.35",
    "serverless-offline": "^3.31.3",
    "serverless-plugin-offline-dynamodb-stream": "^1.0.18"
  }
}
