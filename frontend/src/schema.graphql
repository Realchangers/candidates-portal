type Query {
  user(userName: ID): User
}

type User {
  userName: ID
  password: String
  firstName: String
  lastName: String

  jobOffers(first: Int): [JobOffer]
}

type JobOffer {
  id: ID
  date: String
  title: String
  description: String
  company: String
}
